services:
  document-redactor:
    build:
      context: .
      dockerfile: Dockerfile
    image: redactify:latest
    ports:
      - "8081:8081"
    environment:
      HOST: 0.0.0.0
      PORT: 8081
    env_file:
      - .env
    restart: unless-stopped
